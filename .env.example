# API keys for VLM inference (only needed for evaluation modes that call a VLM).
# Copy this file to .env and fill in the values you need.

# OpenRouter (default VLM provider)
OPENROUTER_API_KEY=

# Hugging Face (for self-hosted vLLM endpoints)
HF_TOKEN=
